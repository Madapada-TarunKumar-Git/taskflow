spring:
  application:
    name: docker-taskflow
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DOCKER_DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}


task:
  scheduler:
    delay: 60000